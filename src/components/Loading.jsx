const Loading = () => (
  <div
    className="loading"
    style={{
      fontSize: "24px",
      fontWeight: "bold",
      textAlign: "center",
      padding: "20px",
    }}
  >
    Loading...
  </div>
);

export default Loading;
